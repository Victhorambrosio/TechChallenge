@model TechChallenge.Professor

@{
    ViewData["Title"] = "Excluir professor";
}

<div class="crud-form-page">
    <div class="crud-form-header">
        <h1 class="crud-form-title">Excluir professor</h1>
        <p class="crud-form-subtitle mb-0">Confirme abaixo se deseja remover este registro.</p>
    </div>

    <div class="crud-form-panel">
        <div class="crud-delete-box">
            <p class="crud-delete-warning mb-3">Tem certeza que deseja excluir este professor?</p>

            <dl class="row crud-delete-list mb-0">
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Nome)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Nome)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Email)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Email)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Especialidade)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Especialidade)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.DataContratacao)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.DataContratacao)</dd>
                <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Ativo)</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Ativo)</dd>
            </dl>
        </div>

        <form asp-action="Delete" class="crud-form-actions">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Excluir" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Voltar para a lista</a>
        </form>
    </div>
</div>
