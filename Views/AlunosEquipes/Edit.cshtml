@model TechChallenge.Models.AlunoEquipe

@{
    ViewData["Title"] = "Editar vínculo";
}

<div class="crud-form-page">
    <div class="crud-form-header">
        <h1 class="crud-form-title">Editar vínculo</h1>
        <p class="crud-form-subtitle mb-0">Atualize a data de entrada ou confirme o vínculo selecionado.</p>
    </div>

    <div class="crud-form-panel">
        <div class="crud-form-body">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="AlunoId" />
                <input type="hidden" asp-for="EquipeId" />

                <div class="mb-3">
                    <label asp-for="DataEntrada" class="form-label"></label>
                    <input asp-for="DataEntrada" class="form-control" />
                    <span asp-validation-for="DataEntrada" class="text-danger"></span>
                </div>

                <div class="crud-form-actions">
                    <input type="submit" value="Salvar" class="btn btn-primary" />
                    <a asp-action="Index" class="btn btn-secondary">Voltar para a lista</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
